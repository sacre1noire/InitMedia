@@ -69,6 +69,14 @@
 			authRoutes.POST("/refresh", authHandler.RefreshTokens)
 			authRoutes.POST("/logout", authHandler.Logout)
 		}
+vi-
+vi-protectedRoutes := api.Group("/protected")
+vi-protectedRoutes.Use(authMiddleware.RequireAuth())
+vi-{
+vi-git-aheadbehind protectedRoutes.GET("/profile", func(c *gin.Context) {
+c.JSON(http.StatusOK, gin.H{"message": "This is a protected route"})
+})
+vi-}
 	}
 
 	// Swagger Endpoint
